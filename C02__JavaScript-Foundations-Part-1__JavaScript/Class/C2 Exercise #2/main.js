const food = {
    Cucumber : .25,
    Ramen : .75,
    ribEye : 49,
};

for (let i = 0; i < users.length; i++) {

    '<li>' + food[0] + '</li>' + '<li>' + food[1] + '</li>' + '<li>' + food[2] + '</li>'

}

console.log('<ul>' + food[i] + '</ul>')